import styled from 'styled-components';
import { systemFont } from 'style/constants';

const H1 = styled.h1`
  font-family: ${systemFont};
  font-size: 42px;
  line-height: 44px;
`;

export default H1;